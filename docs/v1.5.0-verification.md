# v1.5.0 — Post-Release Verification

**Date:** 2026-09-15
**Tag:** v1.5.0
**Tag commit:** 95a74bf
**Verification commit:** <the commit that fixes setup.py>

## Summary

| Section | Result |
|---------|--------|
| 8.1 CI | ✓ PASS |
| 8.2 SQL | ✓ PASS (SQLite + PostgreSQL; MySQL skipped) |
| 8.3 CSV | ✓ PASS (structural; GUI checks skipped) |
| 8.4 CLI | ✓ PASS (after setup.py fix) |
| 8.5 README | ✓ PASS |

## 8.1 — CI

Run ID 34909089525. All six top-level jobs green plus the four-language
wrapper matrix. Success messages confirmed:
- check-sql-export: "All four SQL files match the registry."
- check-csv-export: "All four CSV/TSV files match the registry."
- check-cli: "171 passed" plus three smoke tests
- validate-json: "✅ VALIDATION PASSED"

## 8.2 — SQL

- SQLite: 302 total, 167 active, 135 withdrawn. minor_units=3 list is
  exactly [BHD, IQD, JOD, KWD, LYD, OMR, TND]. CHECK constraints fire on
  invalid status and minor_units=99. Unicode symbol round-trips for KWD.
- PostgreSQL 16 (Docker): same counts, same three-decimal list.
- MySQL: not tested (MySQL not installed on verification machine).

## 8.3 — CSV

All four files: correct BOM policy, LF-only, UTF-8 decodable, header
matches the 11-column schema, 302 rows. Data integrity: numeric_code
3 digits, status active/withdrawn, is_independent lowercase boolean, no
duplicates.

Skipped: pandas load, Excel on Windows, LibreOffice FR/DE, Google Sheets
paste — none available on the verification machine.

## 8.4 — CLI

Wheel iso4217_registry-1.1.0-py3-none-any.whl contains all four expected
files. Installed in a fresh venv; iso4217 --version prints 1.5.0.

All 13 checks in the verification script pass after the setup.py
build_py fix. See the fix commit for the packaging correction.

## 8.5 — README

- File names referenced in README match `ls iso4217.*` exactly.
- All eight subcommands in README match `iso4217 --help` output.
- "Why?" bullets accurately describe what ships.

## Known gaps

- **The v1.5.0 tag points at commit 95a74bf**, which ships a wheel that
  is missing iso4217.json and schema.json. The build_py fix landed after
  the tag. Users who install from a wheel built from the tag will get a
  broken CLI. Users who install from an sdist or use the JSON directly
  are unaffected.
- MySQL, Excel, LibreOffice European locale, Google Sheets paste, and
  pandas load were not verified on the local machine. They are expected
  to work but unconfirmed.

## Follow-ups for v1.5.1 or v1.6.0

- Publish iso4217-registry 1.1.0 (with the build_py fix) to PyPI
- Publish to npm and crates.io
- Complete the skipped GUI checks on a Windows + LibreOffice machine
- Cleanup: hand-edit schema.json to avoid the json.dumps reformatting
- Cleanup: remove wrappers/python/schema.json (duplicate of root) and
  drop schema.json from wrappers/python/MANIFEST.in
- Cleanup: MANIFEST.in references LICENSE but no LICENSE file exists in
  wrappers/python/