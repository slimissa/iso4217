# v1.5.3 Post-Release Verification

## Summary

| # | Check | Result |
|---|-------|--------|
| 8.1 | CI | **NOT RUN — see note** |
| 8.2 | Parquet structure | PASS |
| 8.3 | pandas read | PASS |
| 8.4 | DuckDB typed query | PASS |
| 8.5 | Parquet vs CSV | PASS |
| 8.6 | Parquet vs SQL | PASS |

**Scope note, read before trusting anything below:** this report was produced
in a sandboxed session with no access to the actual `v1.5.3` git tag, the
real GitHub repository, or the `gh` CLI. 8.2 through 8.6 were run against a
local reproduction of the release — the same `iso4217.json` → regenerate →
`--check` sequence from the release steps, executed in this session — not
against the tagged, pushed artifact. If the actual pushed tag's files differ
from what this session regenerated for any reason, this report does not
cover that difference. 8.1 could not be attempted at all; there is no
substitute for it here.

## 8.1 — CI

**Not run.** `gh` is not available in this environment and there is no
network path to the actual repository's Actions runs. This check requires
running `gh run list --limit 1` against the real repository and pasting its
output; that has not happened. Do not treat 8.1 as verified.

## 8.2 — Parquet structure

```
$ python3 -c "
import pyarrow.parquet as pq
t = pq.read_table('iso4217.parquet')
print(t.num_rows, t.num_columns)
print(t.column_names)
print(t.schema.field('minor_units').type)
print(t.schema.field('is_independent').type)
print(t.schema.field('peg_rate').type)
print(t.schema.metadata[b'iso4217.version'].decode())
"
302 11
['code', 'numeric_code', 'name', 'minor_units', 'symbol', 'entity', 'status', 'is_independent', 'pegged_to', 'peg_type', 'peg_rate']
int8
bool
double
1.5.3
```

302 rows, 11 columns, native types unchanged from v1.5.2, footer metadata
reports `1.5.3`.

## 8.3 — pandas read

```
$ python3 -c "
import pandas as pd
df = pd.read_parquet('iso4217.parquet')
print(df.shape)
print(df['is_independent'].dtype)
print(df['minor_units'].dtype)
print(df['pegged_to'].isna().sum())
print(df['peg_rate'].isna().sum())
"
(302, 11)
bool
int8
256
258
```

Shape and null counts unchanged from v1.5.2 — expected, since v1.5.3 made no
currency data changes.

## 8.4 — DuckDB typed query

**Q1:**
```
$ python3 -c "
import duckdb
r = duckdb.query(\"SELECT code FROM 'iso4217.parquet' WHERE is_independent AND minor_units = 3 ORDER BY code\").fetchall()
print([row[0] for row in r])
"
['IQD', 'LYD', 'TND']
```

**Q2:**
```
$ python3 -c "
import duckdb
r = duckdb.query(\"SELECT COUNT(*) FROM 'iso4217.parquet' WHERE pegged_to IS NOT NULL\").fetchone()
print(r[0])
"
46
```

**Q3:**
```
$ python3 -c "
import duckdb
r = duckdb.query(\"SELECT code, peg_rate FROM 'iso4217.parquet' WHERE peg_rate IS NOT NULL ORDER BY peg_rate DESC LIMIT 3\").fetchall()
print([(row[0], float(row[1])) for row in r])
"
[('XAF', 655.957), ('XOF', 655.957), ('KMF', 491.968)]
```

All three match the corrected values from `docs/v1.5.2-verification.md`'s
8.4 correction and the two lock-in tests in `tests/test_export_parquet.py`
(`test_independent_three_decimal_currencies`, `test_top_three_by_peg_rate`).
These values were not copied from that report — they were produced by
running the three queries above in this session.

## 8.5 — Parquet vs CSV

```
$ python3 -c "... symmetric diff on (code, name) across all rows ..."
parquet rows: 302 csv rows: 302
symmetric diff on (code,name): 0
```

## 8.6 — Parquet vs SQL

```
$ python3 -c "... load iso4217.sqlite.sql into SQLite, compare 7 shared columns ..."
rows compared: 302 mismatches: 0
```

Compared `code`, `numeric_code`, `name`, `minor_units`, `symbol`, `entity`,
`status` — the seven columns Parquet and the SQL export share — for all 302
rows. Zero mismatches.

## Known remaining gaps

Carried forward from `docs/v1.5.2-verification.md` where still applicable:

- Windows-specific CSV checks (Excel encoding, CR bytes, BOM policy) and the
  LibreOffice European-locale check have not been run in this environment
  (Ubuntu sandbox, no Windows host available). Same limitation as v1.5.1 and
  v1.5.2 — unresolved, not newly introduced.

New to this report:

- **8.1 (CI) was not attempted**, not merely deferred. Unlike the Windows
  gap above, this isn't an environment limitation that was worked around —
  it simply didn't happen. Someone with access to the real repository needs
  to run `gh run list --limit 1` against the `v1.5.3` release commit and
  either confirm green CI or append the result here.
- **This entire report was produced against a local reproduction, not the
  pushed tag.** Every other verification report in this project (v1.5.0
  through v1.5.2) was run against the actual committed/tagged state. This
  one wasn't, because this session has no access to that state. Whoever has
  repo access should re-run 8.2–8.6 against the real `v1.5.3` tag and either
  confirm these values stand or correct them — the same standard this report
  itself is enforcing on the v1.5.2 report's errors.

## Follow-ups

- Publish to PyPI, npm, crates.io
- Begin v1.6.0 (ISO 3166)
